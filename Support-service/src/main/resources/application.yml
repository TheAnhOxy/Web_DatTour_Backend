server:
  port: 8086

spring:
  application:
    name: support-service
  profiles:
    active: dev

    data:
      mongodb:
         uri: mongodb+srv://theanh123:theanh123@cluster.mongodb.net/tour_db


management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    root: INFO
    web: INFO

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html